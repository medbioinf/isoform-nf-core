{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/Anton-Bch/isoform-nf-core/main/assets/schema_sra_manifest.json",
    "title": "Anton-Bch/isoform-nf-core pipeline - params.sra_manifest schema",
    "description": "Schema for the optional SRA manifest provided with params.sra_manifest",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "condition": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Condition must be provided and cannot contain spaces",
                "meta": ["condition"]
            },
            "replicate": {
                "type": "integer",
                "minimum": 1,
                "errorMessage": "Replicate must be provided as a positive integer",
                "meta": ["replicate"]
            },
            "run_accession": {
                "type": "string",
                "pattern": "^[SED]RR[0-9]+$",
                "errorMessage": "Run accession must be an SRA/ENA/DRA run accession such as SRR123456"
            },
            "strandedness": {
                "type": "string",
                "enum": ["forward", "reverse", "unstranded", "auto"],
                "errorMessage": "Strandedness must be one of 'auto', 'forward', 'reverse' or 'unstranded'; 'auto' passes Salmon --libType A for inference",
                "meta": ["strandedness"]
            },
            "batch": {
                "type": "string",
                "pattern": "^\\S*$",
                "errorMessage": "Batch cannot contain spaces",
                "meta": ["batch"]
            }
        },
        "required": ["sample", "condition", "replicate", "run_accession", "strandedness"]
    }
}
